export const countKeyMapping = {
  total: 'lucette.count.total',
  sameDay: 'lucette.count.same_day',
  schedulePending: 'lucette.count.schedule_pending',
  processPending: 'lucette.count.process_pending',
  standalone: 'lucette.count.standalone',
};
